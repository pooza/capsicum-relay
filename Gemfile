source 'https://rubygems.org'

ruby '~> 4.0.5'

# Web framework
gem 'sinatra'
gem 'puma'

# JSON
gem 'json'

# Database
gem 'sqlite3'

# APNs (HTTP/2)
gem 'apnotic'

# FCM v1 API
gem 'googleauth'

# Web Push decryption (aesgcm / aes128gcm)
gem 'openssl'

# HTTP client (FCM)
gem 'net-http'

# Logging
gem 'logger'

# YAML config
gem 'yaml'

# エラーアラート (capsicum-relay#10)
gem 'sentry-ruby'

group :development do
  gem 'rubocop'
  gem 'rubocop-performance'
end
